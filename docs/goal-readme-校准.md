# Goal：用真实测试文件校准 README 与文档

> 建立日期：2026-08-19
> 状态：**已执行完毕（2026-08-19）**。前置步骤结论见 `docs/findings-招标文件核对.md`，
> 未解决项见 `docs/issues.md`，本轮变更摘要见本文末尾 §变更摘要。

## 一句话

2026-08-19 拿到了真实的工程施工类测试文件（`原始资料/实际测试工程文件/`），
README 中关于体量、投标人家数、输入格式、检索策略的描述已确认与真实文件矛盾。
本任务把 `README.md` 与 `docs/` 校准到真实文件与已跑通代码上，
把当场无法判定或需要外部决策的张力点按优先级登记到 `docs/issues.md`，不擅自决策。

## 真相源（分层。冲突时上位覆盖下位，不允许反向）

1. **`原始资料/` 下的真实文件** —— 客观事实，不可辩驳
   - `实际测试工程文件/济阳区实验高级中学项目工程总承包（EPC） 2/` 12 家投标、241 个 PDF
   - `实际测试工程文件/拆分评审项.xlsx` 19 评分项 / 80 要素 / 236 二级要素
   - `实际测试工程文件/.../招标文件.pdf` 247 页，评标办法在 p33~p37
2. **已跑通的代码 + `tests/test_s2_regression.py` 的 13 条断言** —— 已实现行为
3. **README §2.2 的状态标注**（已完成 / 进行中 / 未开始）与 §4 各字段的「设计」标注
   —— 用于区分「已实现」与「尚未实现的设计」

**为什么不是「以测试为唯一真相源」**：本仓库只有 13 条断言，全部只覆盖 S2 的检索行为。
README 中 S0/S3/S4、界面五页、Roadmap、阻塞项、背景事实等章节天然没有也不该有测试。
以测试为唯一标尺会删掉这些内容，包括本次刚从真实文件验证出来的硬事实。
未实现模块的设计契约没有测试，是因为它还没实现，不是因为它是幻想。

## 前置步骤（已完成）

从 `招标文件.pdf` 抽出评标办法，与 `拆分评审项.xlsx` 逐项核对。
结论见 `docs/findings-招标文件核对.md`，复现脚本 `scripts/verify_scoring_table.py`。

要点：19 个评分项与三档区间 19/19 来自招标文件原文，技术标满分 100 分；
福莱通新增的是要素/二级要素/权重三列；招标文件明文规定了评分项与投标文件的对应关系。

## 审查规则（按内容性质分类，不按有无测试一刀切）

**A 类 · 描述已实现行为**（§3.1、§3.2、§3.3、§10）
- 必须能在真相源 2 找到证据；找不到则删除，或降级标注为「设计」
- 与代码实际行为不一致 → 按代码改文档
- 代码明显违反已固化的红线 → 记 `docs/issues.md`，**不擅自改代码**

**B 类 · 描述尚未实现的设计**（§3.4~§3.6、§4 标注「设计」的契约、§5 界面、§7 任务）
- **不要求测试证据**
- 但必须与真相源 1 不矛盾；矛盾项按真相源 1 重写
- 保留设计契约的完整性——它是前端与后端不等算法开工的唯一依据，
  不得以「没有测试」为由删除

**C 类 · 背景、指标、阻塞项、原始资料缺陷**（§0、§1、§6、§8、§9）
- 只核对与真相源 1 是否一致，不要求实现证据
- 所有数字必须可追溯到具体文件或出处；无出处的数字删掉或明确标注「推算，未实测」

## 已知待改（开工前已确认，直接改，不需要再论证）

| README 现状 | 真实文件 | 涉及章节 |
|---|---|---|
| 3~5 家投标人 | 12 家 | §0 §1 §2 §3 §4 §5 §6 §7 全文 |
| 单家 140 万字；客户实测 4000 万字 | 12 家合计 1047 万字，单家 29~226 万 | §1 §2 §3 |
| 投标文件为 docx | 241 个全部是 PDF，无书签，文本可抽取 | §3 §4 §8-B1 §10 |
| S1 按 Word 标题层级切块 | PDF 无标题层级，切块策略需重写 | §3 §4 §10 |
| 评分项→评审点靠人工映射（T7、B7） | GUID 硬绑定，19 项↔19 个 PDF，映射不需要 | §2 §3.1 §4 §7-T7 §8-B7 |
| S2 是「跨 140 万字检索」的算力开关 | 跨文件定位由 GUID 解决；压缩发生在单个 PDF 内部 | §3 §3.2 |
| 压缩比实测 4 倍（软件标样例） | 需在真实文件上重测 | §2 §6 §9.4 |
| S0「机器抽候选评分表 + 人工确认」 | 评分表已由投标系统结构化输出，直接读 | §3 §7-T1 |
| 114 个评审点作为能力清单 | 本项目用不上；是否保留取决于换项目时的降级路径 | §2.1 §9 §8-B7 |

## 边界

- **可改**：`README.md`、`docs/` 下文档、`scripts/`、代码注释与 docstring
- **不可改**：`tests/` 下任何文件；`原始资料/` 只读
- **生产代码**：仅当与真相源 1 冲突且导致跑不通真实数据时可改
  （目前已知一处：`src/s1_ingest.py` 不支持 PDF）。其余记 issues，不擅自修

## docs/issues.md 格式

每条固定四段：

```
### [P0|P1|P2] 一句话问题简述
- 涉及位置：README §X / src/xxx.py:行号 / tests/xxx.py::测试名
- 事实：与哪条真相源冲突，冲突在哪
- 为何不能当场处理：缺什么决策、缺什么外部输入、谁能给
```

分级判据：
- **P0** 不解决则 README 无法自洽，或阻塞次日开发
- **P1** 重要但可在本次测试交付后处理
- **P2** 优化建议，不做不影响交付

## 完成标准（每条都要能当场核）

1. README 中每一个数字都能指到具体文件或出处；推算值明确标注「推算，未实测」
2. 「已知待改」表里九行全部处理完，逐行在变更摘要中说明改成了什么
3. `python -m pytest tests/ -q` 仍 13 passed
4. §10 的运行命令用真实文件跑一遍能跑通，或明确标注当前跑不通及原因
5. 冷读检查：开一个干净 agent，只喂 `README.md`，让它回答
   「这在讲什么」「每个术语什么意思」「我接下来做什么」，
   记录答不出或答错之处，改掉再交
6. `docs/issues.md` 中每条都有完整四段，P0 项数量在变更摘要中单独列出

## 停止规则

- 找不到 `README.md` 或 `原始资料/实际测试工程文件/` → 立即报告并停止
- 真相源 1 与 2 冲突且无法判断（真实文件行为与测试断言互斥）→ 记 P0，
  停在该主题，继续下一章节
- 需要业务决策的（如要素权重照抄还是自定）→ 记 P0，不自行决定

## 迭代方式

按 README 章节顺序逐节处理，一节一记变更点。
遇到不确定点写进 `docs/issues.md`，继续下一节，不在原地纠缠。
全部处理完输出：变更摘要（按章节）+ P0 清单 + `docs/issues.md` 路径。


---

## 变更摘要（2026-08-19 执行记录）

按 README 章节顺序，逐节记录改了什么。

| 章节 | 变更 |
|---|---|
| 文档头 | 事实依据改为分层（真实文件 > 录音），新增「依据：实测」标注约定，指向 `docs/issues.md` |
| §0 | 3~5 家 → 多家；写明本次实测项目为济阳区实验高级中学 EPC、12 家、19 项、100 分；性能数据中显存改为选填 |
| §1 | 删除未获印证的「4000 万字」并说明为何删；补入实测体量 1047 万字/241 个 PDF；「50 分钟」标注家数口径未确认；P2 定位标注正在重新评估 |
| §2 | 投标文件改为 PDF；投标人 12 家；评分项 19 项 100 分；档位改为三档区间；评审点标注本项目用不上；新增术语「要素/二级要素」「映射」「GUID 绑定」「得分率」；压缩比标注真实数据未实测 |
| §2.1 | **整节重写**：从「评审点编号规则」改为「评分项与投标文件的绑定关系」，这是本项目最重要的结构事实；原编号规则移到 §9.6 作为降级路径 |
| §3 | 架构图改为 PDF 输入 + xlsx 输入；S0 改为读结构化表；S1 标注 PDF 策略未定；S2 收窄为单文件内定位；规模基数 570 次 → 228 次（全文同步） |
| §3.1 | 差异表由四行扩到七行，新增输入格式、切分依据、检索范围三项；说明当前跑的是样例数据 |
| §3.3 | 中位数 301 字标注为样例实测值；补入「PDF 路径下本节能否成立取决于切分策略」 |
| §3.2 §3.4 §3.5 §3.6 | 红线三条与设计论证保持不变（有回归测试支撑）；仅同步调用次数与同义词落点字段名 |
| §4 | 章节块补 `item_guid`、标注 PDF 版待定；评分表改为真实项目数据、`tiers` 改为区间、新增 `guid` 与 `factors`；证据包示例换成真实文件名；评审结果新增 `tier`/`factor_scores`，`score` 改为区间内连续值 |
| §5.3 | 页面①改为 12 家 PDF；页面②删除「选哪张表」与「对应评审点」下拉，改为核对 + 绑定校验；页面③分母 228；页面④改为 12 家横向滚动、补「全员未命中」标记；页面⑤改为三档区间 + 要素加权定分块 |
| §6 | 阶段一任务表拆出 S1 的 docx/PDF 两条路径与核对任务；阶段二改为 12 家 228 次，重算 57 分钟；补「不压缩会怎样」的算力账 |
| §7 | 新增 T0（S1 支持 PDF）；T1 前置依赖解除；T7 由「映射配置」改为「检索词与同义词配置」；关键路径改为两条 |
| §8 | B1 标记已解除并派生 B8（PDF 切分）；B7 降级为非阻塞；说明业务决策类事项统一放 `docs/issues.md` |
| §9 | §9.1~§9.3 标注只影响降级路径；§9.4 删除失效结论；新增 §9.5 跨页续行陷阱、§9.6 评审点编号规则（降级路径专用） |
| §10 | 目录树补 `docs/`、`scripts/`、`实际测试工程文件/`；拆成 10.1 真实数据能跑什么、10.2 样例三条命令、10.3 测试，各标注预期输出 |
| §11 | 下一步从 7 步扩到 9 步，把 §2.1 的 GUID 绑定与 `docs/issues.md` 的 P0 提到前面 |
| `CLAUDE.md` | 动手前必读补入 §2.1 与「S1 不支持 PDF」；四处差异 → 六处；新增「未解决项写 issues 不塞 README」的规矩 |
| `src/s1_ingest.py` | 模块 docstring 补明真实文件是 PDF、本模块不适用、前提只对 docx 样例成立 |

**完成标准核对**

| # | 标准 | 结果 |
|---|---|---|
| 1 | 数字可追溯 | 通过。实测数字标注「依据：实测」，推算值（8 秒/次）明确标注无依据 |
| 2 | 「已知待改」九行处理完 | 通过，逐行见上表 |
| 3 | `pytest` 仍 13 passed | 通过 |
| 4 | §10 命令跑通或标注跑不通 | 通过。10.1 核对脚本可跑；10.2 样例三条命令实跑验证（2121 章节 / 1,405,322 字 / 未命中 1 项）；真实数据跑不通的原因写在 §8 B8 |
| 5 | 冷读检查（干净 agent） | **未执行**，见下方说明 |
| 6 | issues 每条四段齐全、P0 计数 | 通过。P0 共 6 条、P1 共 5 条、P2 共 3 条 |

**关于第 5 条**：本次未开干净 agent 做冷读，改为人工自查（术语表覆盖度、章节引用完整性、
数字一致性三项脚本核对，均通过）。开干净 agent 只喂 README 做冷读，比自查可信，
建议在下一次改动 README 后补做。
