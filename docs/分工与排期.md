# 分工与排期

> 建立日期：2026-08-19　　最后更新：2026-08-19
> 适用范围：本次济南公共资源交易中心 AI 辅助评标测试，目标日期 2026-08-21。
> 团队：3 人——Aiden（算法 + 清障）、后端 1 人、前端 1 人。
> 任务编号 T0~T9、阻塞项编号 B2~B8 的定义在 `README.md` §7 与 §8，本文不重复。
> 未解决项见 `docs/issues.md`，本文不重复。
> 本文只管**谁做、什么时候做完、做到什么程度算完成**；技术方案一律以 `README.md` 为准。

---

## 0. 排期成立的前提（读排期表之前先看这条）

**本文的排期按「8/21 是客户现场测试日、不可推迟」倒排。这个前提尚未确认。**

- **若前提成立**：§4 的死线全部是硬死线，§5 的砍单不是保险而是**主计划**，
  必须在 8/20 中午之前决定砍哪几样，不能等落后了再砍。
- **若 8/21 只是内部目标日、可推迟**：所有死线整体后移，§5 的砍单退回保险位置。

**解除条件**：向陈老板或客户确认 8/21 的性质，把结论写回本节并删掉这段说明。
**责任人 Aiden，死线 8/19 下班前**——这条不确认，下面整张排期表都是猜的。

---

## 1. 分工原则

**三个人的盘子里，能合并的模块必须合并。**

- **S4 报告不设独立模块，并入前端。** 报告的数据层是 `README.md` §4 已冻结的
  「报告数据」JSON，渲染层就是页面④加上性能数据。让前端顺手导出静态 HTML，
  比后端另写一个渲染模块省一整个人天。
- **性能埋点不设独立任务，写进 S3 的调用封装。** 每次调用的 token 与耗时落盘，
  是 S3 本来就要做的事。

**三条线的划分依据是「卡在什么上」，不是「模块归属」：**

| 线 | 负责 | 卡什么 |
|---|---|---|
| Aiden | 清障 + 模型侧（B2、B3、T1、T2、T7、T9） | B2 模型端点未到手 |
| 后端 | 数据管道（T0 → T8 → T3） | 不卡。切分策略已定案（README §3.7） |
| 前端 | 界面 + 报告（T5 → T6 → 报告导出）+ 造 mock（§2） | 不卡。只依赖 README §4 的数据契约 |

**但三条线不是互不相干的。** 真实的关键路径是一条串行链，它串起全部三个人：

```
T0 ──→ T8 ──→ T2 ──→ 全量演练 ──→ 交付
（后端）  （后端）  （Aiden）    （三人）
                ↑
                B2（外部，唯一不可控项）
```

前端的 T5/T6 确实可以并行推进（靠 §2 的 mock 顶着），但**最终交付物必须走完这条串行链**。
过去把两条线描述成「互不阻塞」掩盖了这一段——它们在 T2 交汇，而交汇点原本排在交付当天。
§4 的排期把交汇提前到 8/20 晚，就是为了给全量演练腾出时间。

**当前负载明显不均，这是已知风险不是疏漏：**
后端和前端的任务都是「照着已定案的规则实现」，工时可估；
Aiden 这条线有 4 个任务 + 2 个阻塞项清障，其中 **T2 的 prompt 调优是全项目工时最不可预测的一块**
（要调几轮无法提前知道），而它的前置 B2 还是唯一的外部不可控项。
减压手段见 §3.3 的 T1 条。

---

## 2. 测试数据用真的，只有一处例外

**S1、S2 的中间产物一律跑真实文件，不手写假数据。**

真实文件已在 `原始资料/实际测试工程文件/` 下（12 家、241 个 PDF）。
后端 T0 完成后，`data/interim/sections.json` 与 `located.json` 即为真实数据，
前端直接读。手写的假 JSON 一定和真实输出对不上（字段缺失、路径杜撰、字数不真实），
照它做完还要返工一次。

**唯一的例外是 S3 的输出 `review_results.json`。**

原因有两条：

1. **物理上不存在**——B2 未解除，模型一次都调不了。前端的页面④⑤要显示判分、理由、
   置信度、`unrated` 与 `⚠` 标记，这些只能来自 S3。
2. **部分状态真跑也难凑**——`unrated`（需模型连续失败三次）、`confidence < 0.85`
   （README §4 的阈值，走了降级检索或两个减分因素叠加才触发）、某评分项 12 家全部未命中。
   这些边界态前端必须做对（`0` 与 `—` 混淆会误导评标，见 README §5.3 页面④），
   但正常跑一遍未必碰得到，本来就该构造。

**谁造：前端自己造。** 用它的人最清楚需要哪些边界态，转手一道反而失真。
**什么时候造：8/19 内**，不等后端的真实数据。

**构造方式：照着一份真实的 `located.json` 生成，不凭空编。**
`bidder`、`item_id`、`item_guid`、`cite` 的编号范围、证据文件名全部取自证据包，
只有 `score` / `tier` / `reason` / `confidence` 是编的。
额外塞入三条边界态：一条 `unrated`（`score` 必须为 null 不是 0）、
一条 `confidence < 0.85`、一项全家未命中。

**8/19 当天用哪份 `located.json` 打底**：T0 还没完成，用早期软件标样例产出的那份
（`原始资料/公司的临时样例文件仅作参考/`，结构正确、内容不对，见 README §9.4）。
**结构对了就够**——mock 要的是字段齐全和边界态，不是内容真实。
T8 产出带 `bidder` / `item_id` 的真实证据包后（8/20 中午），前端把 mock 重新生成一次。

产出位置 `data/interim/mock/review_results.json`。**B2 解除、T2 跑通后立即删除。**

---

## 3. 三条线的任务与验收

验收条件写「做到什么程度算完成」，没有验收条件的任务不算任务。
状态词判定标准见 `README.md` §2.2（已完成 = 代码写了 + 代表性数据跑通 + 人工检查过输出）。

### 3.1 后端 · 数据管道

**T0 · S1 支持 PDF 切块**　死线 8/19 下班（全项目最紧的一环，前端与 T8 都在等）

- 规则已在 `README.md` §3.7 定案，纯实现：行首编号定层级（五级）、
  行长 < 0.8 × 该文档行长 P90 判标题、文件名尾部 GUID 写入 `item_guid`。
- 验收：12 家 × 20 个 PDF 全跑通不报错；输出符合 README §4 章节块结构；
  **块字数中位数落在 100~150 字**（探针实测值为 114 字，偏离过大说明规则实现有误）。
- **附带一件小事，别漏**：章节块要记录页码。README §5.3 页面⑤ 的「在原文中定位」
  依赖它跳转到对应 PDF 页，而 §4 的章节块结构目前没有这个字段。
  切分时顺手写入的成本是零，事后补要重跑 241 个 PDF。字段名与 README §4 同步确认。

**T8 · S2 适配多投标人与单文件检索**　死线 8/20 中午

- 检索范围从「一家的全部章节块」收窄到「评分项对应的那一个 PDF」；
  输出补齐 `bidder` / `item_id` / `item_guid`。
- **收窄检索范围会改变 DF 的统计基**：当前 DF 算在「一家 2121 块」上，
  收窄后单份 PDF 中位只有 111 块，`ANCHOR_DF_RATIO = 0.05` 就等于 5.5 块。
  **动手前先跟 Aiden 确认 DF 算在哪一层**（单个 PDF / 该家 20 个 PDF / 12 家全部），
  README 目前没写这个，猜错会让 §3.2 红线一的前提失效。
- 验收：`python -m pytest tests/ -q` 仍 13 passed；12 家 × 19 项跑出证据包；
  满足 README §6 阶段一的三条 S2 效果指标——
  **证据包非空率 ≥ 90%**、**证据总字数 ≤ 19 × budget**（budget 3000 时即 ≤ 5.7 万字/家）、
  **随机抽 10 项人工确认证据与评分项主题相关**。
  （原「压缩比 ≥ 20 倍」已在 README §6 作废，不要再拿它验收：
  证据字数受 budget 硬约束，那个数量不出检索准不准。）

**T3 · 两层并发调度 + 断点续跑**　死线 8/20 下班

- 模型调用用 mock 顶替（随机 5~30 秒、1/20 概率返回格式错误的 JSON），不等 B2。
- 验收：并发路数可配置；kill 进程后重启不重跑已完成项；
  mock 的坏 JSON 能触发 README §3.6 的三次重试（退避 2/4/8 秒）并最终落 `unrated`，
  且该项 `score` 为 null 而非 0。
- **注意**：这里只能验「机制对不对」。**真实并发路数上限取决于显存（B3）**，
  B3 不解除就只能配一个猜的值，到全量演练时才知道扛不扛得住。

### 3.2 前端 · 界面与报告

**T5 · 页面①②③**　死线 8/20 中午

- 重头是页面③，客户现场盯着看的就是它。**转圈 loading 等于没做**，
  要逐条滚动、带时间戳、带结果。进度分母为 228（12 家 × 19 项）。
- 验收：三页可点击跳转；每完成一项 1 秒内出现在滚动区；
  连续 30 秒无新条目时显示当前处理项名与已等待时长（README §5.1）。

**T6 · 页面④⑤ + 报告导出**　死线 8/20 下班

- 页面④是 12 家 × 19 项，一屏放不下：**表格横向滚动、评分项列固定**。
- 验收：`0`（投标人未写）与 `—`（未评定）视觉可区分；点分数进详情页；
  导出能生成含并排表、未评定单列、性能数据的静态 HTML。

**造 mock**　死线 8/19 下班——规格、打底数据与销毁条件见 §2。

### 3.3 Aiden · 清障与模型侧

**B2 端点**　**死线 8/20 中午**——没人能替，且卡着整条模型线。

- 解除条件：能用该端点跑通一次带 JSON 输出的调用并拿到 token 计数。
- **过了 8/20 中午还没到手，立刻升级**（向陈老板 / 客户提出），不要继续等。
  原因：B2 → T9 → 并发路数 → 全量演练，这条链后面还有一整天的活，
  8/20 下午到手已经是极限；再晚就没有任何调 prompt 和重跑的余量。
- **兜底方案（若 8/20 下班仍未到手）尚未确定**，这是当前最大的敞口。
  待确认的选项：换一个临时端点自测、或把交付内容降级为「S1+S2 全量 + S3 少量样本」
  并提前向客户说明。**这条必须在 8/20 中午升级时一并谈定，不能拖到 8/21 现场。**

**B3 算力规格**　与 B2 同步追，不单独设死线。

- 决定 T3 能配多少并发路数。若 B2 端点是远程托管、我方采不到显存，
  按 README §8 的 B3 处理：数据由对方提供，采不到就留空、不阻塞交付。
- **但要提前想清楚**：采不到显存时，README §6 阶段二「不因显存溢出中断」这条验收
  就无从判定，需要跟客户换一个可验证的说法（如「不因端点限流或超时中断」）。

**T1 · 评分表 + 项目特征摘要**　死线 8/20 中午

- 离线产出，不计入 50 分钟（README §1.1），因此允许人 + AI 协作，不要求全自动。
  产出 `config/projects/济阳区实验高级中学.yaml`，
  可用 `scripts/verify_scoring_table.py` 对照校验。
- **减压开关**：这是 Aiden 线上唯一「不依赖任何人、规格清楚」的活。
  **若 8/20 上午 T1 仍未开工，立刻移交后端**（后端此时 T8 已交付、T3 未开始，有空档），
  Aiden 全力盯 B2 与 T2。移交时把 README §4 评分表结构和核对脚本一起交过去。

**T2 · S3 评审引擎**　骨架 8/19 起，**接真实端点死线 8/20 下班**

- 内容：prompt 设计、README §3.6 的重试逻辑、性能埋点。
- **B2 未到也能先写**：用 mock 模型跑通骨架，端点到手后接上即可。
- 验收：单项跑通并输出 README §4 的评审结果 JSON；
  人为注入格式错误能触发重试并最终标记 `unrated`；越界的 `cite` 编号能触发重试。
- **prompt 里有两处 README 尚未定的规则，写之前先定**（定了就写回 README，别只留在代码里）：
  ① `tier` 由模型判还是由要素得分率算出来，两者冲突时以谁为准；
  ② 档位区间首尾相接（优 3.0-4.0 / 良 2.0-3.0），判到 3.0 分时算哪一档。
  这两条都直接撞 README §1 的 P0 红线「档位不能错」。

**T7 · 检索词与同义词配置**　死线 8/20 中午（与 T8 同步，T8 要用）

- 19 个评分项的检索词（取自 T1 产出的要素与二级要素描述）与同义词扩展填完并人工确认。
- **不能省**：README §3.2 红线二明确「同义词问题的正确解法是在评分表的 `synonyms` 字段里补，
  不是让检索靠换词去猜」。T7 没做，T8 跑出来的证据包质量无从谈起，
  而 T8 的验收（三条效果指标）恰恰依赖它。

**T9 · 模型单点性能实测**　B2 解除后的第一件事，当天完成

- 重点不只是单次耗时，**要测并发 N 路时显存是否扛得住**（README §6 阶段二）。
- 产出直接决定 T3 配几路并发，以及 budget 要不要从 3000 往下调。

---

## 4. 排期

**倒排。** 锚点是 8/21 上午的全量演练——README §6 阶段二的验收标准
（12 家全量跑完 ≤ 50 分钟）本身就要花掉 50 分钟，且第一次跑几乎不可能一次过：
显存不够要降并发、格式错要调 prompt、断点续跑要验，**每验一轮就是 50 分钟**。
把它当成一个要占整个上午的一等任务来排，而不是「联调」里顺手做的事。

由此倒推：**所有开发在 8/20 下班前收口，8/21 不写新功能。**

| 日期 | Aiden | 后端 | 前端 |
|---|---|---|---|
| 8/19（今日） | 确认 §0 前提；追 B2/B3；T2 骨架（mock 模型） | **T0（含页码字段）** | T5；**造 mock（§2）** |
| 8/20 上午 | T1（未开工则移交后端）；T7 | T8（跟 Aiden 对齐 DF 统计基） | T6 |
| 8/20 中午 | **B2 死线——未到手立刻升级** | T8 交付 → 前端换真实数据 | 重新生成 mock；T5 交付 |
| 8/20 下午 | B2 到手则插入 T9；T2 接真实端点 | T3 并发与断点续跑 | T6 收尾；报告导出 |
| 8/20 下班 | **全部开发收口，三线合流联调** | 同左 | 同左 |
| 8/21 上午 | **全量演练（12 家 × 19 项，计时）** ——三人一起看，出问题当场定「修还是砍」 | | |
| 8/21 下午 | 按演练结果修 + 第二轮演练；交付 | | |

**8/21 上午演练要盯的三件事**（对应 README §1 的 P0）：

1. **总耗时**——超 50 分钟就降 budget 或加并发，两条路都要在演练前想好怎么调。
2. **未评定数**——不为 0 就查是 prompt 问题还是端点不稳，前者可修后者只能重试。
3. **页面③ 的滚动是不是真的实时**——这是「不作弊」的唯一可见证据，卡住等于白跑。

---

## 5. 落后了砍什么（按此顺序）

**若 §0 的前提成立（8/21 不可推迟），这一节不是保险，是主计划——
砍不砍要在 8/20 中午决定，不能等 8/21 演练崩了再砍。**

1. **断点续跑**——挂了重跑也就 50 分钟，是保险不是功能。
2. **页面⑤的原文定位跳转**——能显示引用原文即可，跳转高亮是加分项。
   （T0 的页码字段仍然要写，成本为零，砍的只是前端跳转。）
3. **要素加权定分**——直接让模型在档位区间内给分，
   删掉 README §4 的 `factor_scores` 字段与页面⑤的「区间内定分」块。
   **只砍判分侧。** 要素分解有两个用途，检索侧的那个不能砍——
   T1 产出的要素/二级要素是 T7 检索词的来源，也是 S2 的检索粒度（README §4）。
   连检索侧一起砍掉，T7 和 T8 一起塌。

三样全砍掉，仍然满足 README §1 的两条 P0：真实跑完、判分有原文出处可查。

**不能砍的：**

- 页面③的实时滚动——P0「不作弊」的唯一可见证据。
- 未评定（`—`）与 0 分的区分——混淆会误导评标。
- 每个判分的原文引用——P1 考核项。
- **8/21 上午的全量演练**——这不是开发任务，是验收动作本身。
  砍掉它等于把第一次全量运行放在客户面前，那是 P0「真实跑完」的最大风险来源。
