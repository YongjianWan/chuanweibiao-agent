# 项目进度状态盘点

> 建立日期：2026-08-20（晚）
> 视角：**项目经理（Aiden）**——只管状态、阻塞、验收判定，不管技术细节。
> 事实来源：`git log` 已合并提交、`docs/分工与排期.md`、`README.md` §7 任务表。
> 判定标准：README §2.2——**已完成 = 代码写了 + 代表性数据跑通 + 人工检查过输出**，三者缺一不算。
> 本文是 `docs/分工与排期.md` 的状态快照，不替代它；差异以本文为准，分工与排期随后同步。

---

## 0. 当前主线状态

| 项 | 值 |
|---|---|
| 当前 `main` HEAD | `81ca53b` Merge branch 'feat/backend-prototype' — T3 并发调度 + 断点续跑 |
| 已合并功能线 | T0（S1 PDF）、T8（S2 多投标人）、T2（S3 评审）、T3（并发调度）、T1/T7（业务配置）、T9（性能实测） |
| 测试 | `pytest` 全绿（81 passed, 2 skipped） |
| 三条开发分支关系 | `feat/backend-prototype`、`feat/t2-s3-review`、`prototype-t8-seed` 均已合入 `main`，无未合并提交 |

**结论：四条线的开发任务（T0/T8/T2/T3/T1/T7/T9）在代码层面全部收口，主干测试全绿。**

---

## 1. 任务状态总表

### 1.1 后端 · 数据管道（已实现线）

| 任务 | 状态 | 完成判定依据 | 备注 |
|---|---|---|---|
| **T0** S1 支持 PDF 切块 | ✅ 已完成 | `5668d40` + README §7 标已完成；240 PDF 跑通 32,129 块 / 39.4s；`page` 字段已补 | 无遗留 |
| **T8** S2 适配多投标人 + 单文件检索 | ✅ 已完成 | `8dee0c8` `4bc1a80`；检索收窄到单 PDF、DF 统计基取该家全部 20 个 PDF（红线一）；输出补齐 `bidder`/`item_id`/`item_guid` | 无遗留 |
| **S2 换行匹配修复** | ✅ 已完成 | `e3cad39` 已推远程；69→81 测试通过；新增跨 LF/CRLF 回归测试；原始 `text` 保留 | 今日补，独立于原计划编号 |
| **T3** 并发调度 + 断点续跑 | ✅ 已完成 | `5decaf7` → 合入 main；`src/scheduler.py` + `tests/test_s3_scheduler.py` 8 单测；manifest 原子落盘（`temp→fsync→rename`）、断点续跑验证过 | 无遗留 |

### 1.2 模型侧 · S3 与性能实测

| 任务 | 状态 | 完成判定依据 | 备注 |
|---|---|---|---|
| **T2** S3 评审引擎 | ✅ 已完成 | `35af2a0` + `6f0cc0f`(#14) 合并；重试/未评定/两种未命中/`cite` 越界全实现；要素加权定分已拆除 | 无遗留 |
| **T9** 模型单点性能实测 | ✅ 已完成（推断） | `ce7d486 docs(readme): T9 实测数据写回 §6 阶段二`；端点接入见 `b4676cd feat(t2): 接入智能体工厂端点，解除 B2` | B2 解除的硬证据 |

### 1.3 Aiden · 清障、决策、业务配置

| 任务 | 状态 | 完成判定依据 / 待办 |
|---|---|---|
| **T1** 评分表 + 项目特征摘要 | ✅ 已完成 | `c6550ef` 补 19 项 `criteria`；`config/projects/济阳区实验高级中学.yaml` 产出 |
| **T7** 检索词与同义词 | ✅ 已完成 | `e366ed6` 补 19 项 synonyms 229 条，未命中/降级归零 |
| **B2** 模型端点 | ✅ 已解除 | 由 T9 实测反推 |
| **B3** 算力规格 | ⚠️ **待拍板** | 远程托管则报告 `compute_notes` 写"未采集"不阻塞；需确认端点形态并定口径 |
| **B5** 后端人选 | ⚠️ **文档未同步** | 实际后端任务 T0/T8/T3 均有人完成，但 `分工与排期.md` §3.3 仍写"仍缺后端具体是谁" |
| **验收签字** | ⚠️ **待项目经理判定** | 按 §3.3 应由 Aiden 判定，非实现者自评；需逐项核对输出 |

### 1.4 前端 · 界面与报告

| 任务 | 状态 | 完成判定依据 / 待办 |
|---|---|---|
| **T5** 页面①②③ | 🟡 有原型提交，未标完成 | `c01bbc7`/`8f14327` 等 prototype 提交存在；但 §3.2 验收栏无"已完成"标记 |
| **T6** 页面④⑤ + 报告导出 | 🟡 同上 | prototype 分支有对齐提交；验收（12家并排、`0`与`—`区分、导出HTML）未标完成 |
| **造 mock** | ✅ 应有 | §2 要求 8/19 内造，B2 解除后删除；前端自管 |

---

## 2. 阻塞项总览（README §8）

|B#|事项|状态|说明|
|---|---|---|---|
|B1|真实文件未拿到|✅ 已解除（8/19）|`原始资料/实际测试工程文件/` 已在手|
|B2|模型端点|✅ 已解除|T9 实测即硬证据|
|B3|算力规格|⚠️ 待拍板|远程托管则显存"未采集"不阻塞|
|B4|命名不符实际内容|⚠️ 待澄清|非技术阻塞，对外口径问题|
|B5|后端人选|⚠️ 实际已做，文档未同步|需回填 `分工与排期.md`|
|B6|前端技术栈|⚠️ 静态原型阶段不阻塞|确定后写入 `ui-spec.md` §2|
|B7|评审点原始资料缺陷|✅ 降级非阻塞|评审点库本项目不用|
|B8|S1 不支持 PDF|✅ 已解除|T0 完成|

---

## 3. 与 `分工与排期.md` 的差异（需回填）

`docs/分工与排期.md` 仍停在 8/19 的"进行中/死线"措辞，与已合并的 `main` 严重不符。需同步的位置：

1. §3.1 T0/T8/T3 状态 → 标"已完成"
2. §3.3 T1/T7 → 标"已完成"；B2 → 标"已解除"；B5 → 改为"已解除（实际后端任务均完成，待补人名）"
3. §3.4 T2/T9 → 标"已完成"
4. §4 排期表 → 整体标注"开发已收口，待 8/21 全量演练"
5. §3.2 前端 T5/T6 → 等前端负责人按验收栏给判定后再标

---

## 4. 项目经理待办（按优先级）

| # | 动作 | 负责人 | 风险 |
|---|---|---|---|
| 1 | 回填 `分工与排期.md` 状态（纯文档，零风险） | Aiden | 信息债，不回填下一人发懵 |
| 2 | 拍板 B3 算力口径（远程托管→"未采集"） | Aiden + 客户 | 影响报告 `compute_notes` |
| 3 | 前端 T5/T6 验收判定 | 前端 + Aiden | 原型提交 ≠ 验收通过 |
| 4 | 后端/模型侧产出逐项人工核对签字 | Aiden | README §2.2 硬性要求 |
| 5 | 8/21 上午全量演练（12家×19项计时） | 全员 | 验收动作本身，不可砍 |

---

## 5. 一句话结论

**代码层面四条线全部收口、主干测试全绿、三条分支均已合入 main。剩余的是项目经理的"收口动作"：文档回填、B3 口径拍板、前端验收判定、产出签字——以及 8/21 上午那场决定交付成败的全量演练。**
