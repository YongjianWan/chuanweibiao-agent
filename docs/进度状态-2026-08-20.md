# 项目进度状态盘点

> 建立日期：2026-08-20（晚）　**更新：2026-08-20 深夜（T10/T11 已实现并自测通过，实现线全部打通）**
> 视角：**项目经理（Aiden）**——只管状态、阻塞、验收判定，不管技术细节。
> 事实来源：`git log` 已合并提交、`docs/分工与排期.md`、`README.md` §7 任务表、前端状态回报（2026-08-20 晚）。
> 判定标准：README §2.2——**已完成 = 代码写了 + 代表性数据跑通 + 人工检查过输出**，三者缺一不算。
> 本文是 `docs/分工与排期.md` 的状态快照，不替代它；差异以本文为准。

---

## 0. 当前主线状态

| 项 | 值 |
|---|---|
| 当前 `main` HEAD | `2cdc83d`（PR #18 已合入：scheduler/s4_report 全量 perf 口径 + 前端接真实结果） |
| 已合并功能线 | T0（S1 PDF）、T8（S2 多投标人）、T2（S3 评审，PR #16）、T3（并发调度 + **真实端点并发全量**）、S4 数据层（T6 前半）、T1/T7（业务配置）、T9（性能实测） |
| 测试 | `pytest` 全绿（**117 passed**，含 T10 服务层冒烟 6 项） |
| 分支关系 | **四个远端分支全部是 `main` 的子集**（`git rev-list --left-right --count main...<branch>` 右侧均为 0），无游离代码，无 open PR。`fix/prototype-criteria` 已删 |
| 工作区 | **干净**（此前的 `scheduler.py`/`data-contract.md`/`.gitignore` 在飞改动已全部提交入库） |

**三轮真实全量实测（12 家 × 228 项，智能体工厂端点）：**

| 轮次 | 口径 | 总耗时 | unrated | 说明 |
|---|---|---|---|---|
| 第一轮 | 串行 | 8.24 分钟 | 0 | 早期验收（`run_full_12.py`） |
| 第二轮 | 串行，含 T7 synonyms | 10.64 分钟 | 0 | PR #16 验收数据；not_found 0、no_file 0，已对照本地产物核实 228/228 有分 |
| **第三轮** | **并发（scheduler 接真实端点）** | **186 秒（3.1 分钟）** | **0** | `1af269c` 已入库；**README §6 尚未写回此数（仍写 10.64 分钟串行）** |

**结论（2026-08-20 深夜二次修正）：实现线已全部打通。**

此前「开发全部收口」的说法漏了运行时服务层——浏览器与流水线之间没有任何一层。
该缺口当晚补录为 T10/T11 并**已实现自测通过**：`src/server.py`（Python 标准库，零新依赖，
四个 API + 静态站点）+ `prototype/js/app.js` 的实时取数。从浏览器点一下到出报告是一条完整链路，
不再需要 `scripts/build-prototype-mock.js` 人工搬运。

**顺带修掉一个此前没人发现的坑**：`manifest.json` 从来不是任何脚本产出的
（`data/projects/jiyang-epc/manifest.json` 是 8/19 手工写的），而 `merge_sections.py` 与
`s2_locate.py` 都依赖它——换一个新的输入目录必崩。现由 `src/s1_ingest.py --project` 生成。

**剩余全是验收动作**：T5/T6/T10/T11 四条待 Aiden 判定、T7 签字、B3 补数，以及 8/21 上午全量演练。

### ⚠ 当晚发现的 P0 风险：并发 12 路会把端点打崩

经服务层以并发 12 跑真实端点全量：**228 项全部 `unrated`**，684 次重试全失败，
端点返回 `HTTP 500 Agent 'agent-14748c8d' not initialized.`。
同一时刻单次调用正常（`scripts/ping_endpoint.py` 0.9 秒返回），所以是并发压垮的，不是宕机。
改并发 4 跑单家：**19/19 全部 rated，10 次重试，55 秒**。

处置：`src/server.py` 默认并发已定为 **4**；README §6 阶段二加了完整记录；
新增 `scripts/ping_endpoint.py`，**演练当天开演前必须先跑**。
按 4 路外推全量约 9~11 分钟，50 分钟预算仍有 4 倍以上富余。

**这条推翻了此前「并发 12 路 186 秒」的交付口径**，对外报性能数字时按 4 路口径说。

---

## 1. 任务状态总表

### 1.1 后端 · 数据管道（已实现线）

| 任务 | 状态 | 完成判定依据 | 备注 |
|---|---|---|---|
| **T0** S1 支持 PDF 切块 | ✅ 已完成 | `5668d40`；240 PDF 跑通 32,129 块 / 39.4s；`page` 字段已补 | 无遗留 |
| **T8** S2 适配多投标人 + 单文件检索 | ✅ 已完成 | `8dee0c8` `4bc1a80`；DF 统计基取该家全部 20 个 PDF（红线一）；§6 阶段一三条指标全达标 | 无遗留 |
| **S2 换行匹配修复** | ✅ 已完成 | `e3cad39`；跨 LF/CRLF 回归测试 | 今日补，独立于原计划编号 |
| **T3** 并发调度 + 断点续跑 | ✅ 已完成 | `5decaf7` 合入；`1d69410` 修复续跑丢结果；`443355a` 接真实端点（`--agent-factory`）；**并发全量实测 186 秒（`1af269c`）** | 无遗留 |

### 1.2 模型侧 · S3 与性能实测

| 任务 | 状态 | 完成判定依据 | 备注 |
|---|---|---|---|
| **T2** S3 评审引擎 | ✅ 已完成 | PR #16 已合（`62a5c0e`）；重试/未评定/两种未命中/`cite` 越界全实现；要素加权定分已拆除 | **#14 闭环** |
| **T9** 模型单点性能实测 | ✅ 已完成 | 三轮实测见 §0 表；并发 186 秒、unrated 0 为最终口径 | **README §6 待写回第三轮数据**（评审建议：三轮并留，说明 synonyms 与并发各自的账） |

### 1.3 Aiden · 清障、决策、业务配置

| 任务 | 状态 | 完成判定依据 / 待办 |
|---|---|---|
| **T1** 评分表 + 项目特征摘要 | ✅ 已完成 | `c6550ef`；`config/projects/济阳区实验高级中学.yaml` 产出 |
| **T7** 检索词与同义词 | 🟡 **待人工确认** | 229 条已填、第二轮实测未命中/降级归零；**「人工检查过输出」未做，按 §2.2 不能标完成** |
| **B2** 模型端点 | ✅ 已解除 | 智能体工厂端点，三轮实测为硬证据；端点不返回 token 计数，本地估算（第二轮 in ≈ 204.8 万 / out ≈ 8.3 万）并在报告标注 |
| **B3** 算力规格 | 🟡 **待补数据** | 归属已定（神思自有算力，8/20 拍板）、远程托管口径 README §6 已备好；**缺的只是卡型号与显存数字** |
| **B5** 后端人选 | ⚠️ 文档未同步 | 后端任务实际均完成，`分工与排期.md` §3.3 措辞待回填 |
| **验收签字** | ⚠️ 待项目经理判定 | 按 §3.3 应由 Aiden 判定，非实现者自评 |

### 1.4 前端 · 界面与报告（2026-08-20 晚前端回报 + 本仓核实）

**注意：前端在另一台机器（`E:/chuanweibiao-agent`）作业，继续改之前必须先 pull**——其回报里的「未提交修改」核实已在 main（`a27a471`）。

| 任务 | 状态 | 完成判定依据 / 待办 |
|---|---|---|
| **T5** 页面①②③ | 🟡 实现基本成型，待验收 | 五页 Hash 路由可点；实时滚动日志、计时、重试、未评定、token、「显存未采集」口径都有。等 Aiden 按验收栏判定 |
| **T6** 页面④⑤ + 报告导出 | 🟡 实现基本成型，待验收 | S4 数据层已提交（`28ca859`）；矩阵/横滚/固定列/`0`与`—`区分/详情页/导出 HTML（含 `compute_notes`）均已实现。**原硬缺口 `miss_reason` 0⚠ 二级渲染已由 `b2fa5a9` 修复**，验收时复核即可。页面⑤ PDF 页跳转高亮未做——砍单列表第 1 位，可砍 |
| **造 mock** | ✅ 已完成并销毁 | `data/interim/mock/` 已删（`1c5ec93`）；`scoring_reference.json` 非 mock，移至 `data/interim/` 并同步脚本路径（`17c00b5`） |

---

## 2. 阻塞项总览（README §8）

|B#|事项|状态|说明|
|---|---|---|---|
|B1|真实文件未拿到|✅ 已解除（8/19）|`原始资料/实际测试工程文件/` 已在手|
|B2|模型端点|✅ 已解除|三轮实测即硬证据|
|B3|算力规格|🟡 待补数据|归属已定、口径已备，缺卡型号/显存数字|
|B4|命名不符实际内容|⚠️ 待澄清|非技术阻塞，对外口径问题|
|B5|后端人选|⚠️ 实际已做，文档未同步|回填人名即可|
|B6|前端技术栈|⚠️ 静态原型阶段不阻塞|确定后写入 `ui-spec.md` §2|
|B7|评审点原始资料缺陷|✅ 降级非阻塞|评审点库本项目不用|
|B8|S1 不支持 PDF|✅ 已解除|T0 完成|

---

## 3. 与 `分工与排期.md` 的差异

`分工与排期.md` 文首已加状态横幅指向本文（2026-08-20 晚）；§3/§4 的 8/19 措辞保留为历史记录，**当前状态以本文为准**。

---

## 4. 项目经理待办（按优先级）

| # | 动作 | 负责人 | 风险 |
|---|---|---|---|
| 1 | ~~回填 `分工与排期.md` 状态~~ **已做（横幅式）** | Aiden | — |
| 2 | B3 补卡型号/显存数字（采不到按远程托管口径写「未采集」） | Aiden + 客户 | 影响报告 `compute_notes` |
| 3 | 前端 T5/T6 验收判定（0⚠ 已修，验收时复核） | 前端 + Aiden | 原型提交 ≠ 验收通过 |
| 4 | T7 synonyms 与后端/模型侧产出逐项人工核对签字 | Aiden | README §2.2 硬性要求 |
| **0** | ~~T10 + T11~~ **已实现自测通过**（`src/server.py`）。改为**验收动作**：按 README §7 的完成判定各判一次 | Aiden | 低。实现已通，剩判定 |
| 5 | **8/21 上午全量演练**（12 家 × 19 项计时，界面 + 计时 + 客户视角） | 全员 | 验收动作本身，不可砍 |
| 6 | ~~仓库收尾~~ **已全部完成**：`fix/prototype-criteria` 已删、`data/interim/mock/` 已删、~~README §6 写回第三轮 186 秒并发数据~~ **已写回**（三轮并留，含并发下 71.5% 重试率的说明） | 后端 + Aiden | 已清 |

---

## 5. 一句话结论

**实现线全部打通：主干 117 项测试全绿；S0~S4 全量跑通；T10/T11 落地后，
从浏览器点一下到出报告是一条完整链路，页面③是本次运行的实时进度而非回放。**
剩余全是验收动作（T5/T6/T10/T11 判定、T7 签字、B3 补数）与 8/21 上午的全量演练。
