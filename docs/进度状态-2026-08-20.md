# 项目进度状态盘点

> 建立日期：2026-08-20（晚）　**更新：2026-08-20 18:05（对照 git 实况 + 前端状态回报校正）**
> 视角：**项目经理（Aiden）**——只管状态、阻塞、验收判定，不管技术细节。
> 事实来源：`git log` 已合并提交、`docs/分工与排期.md`、`README.md` §7 任务表、前端状态回报（2026-08-20 晚）。
> 判定标准：README §2.2——**已完成 = 代码写了 + 代表性数据跑通 + 人工检查过输出**，三者缺一不算。
> 本文是 `docs/分工与排期.md` 的状态快照，不替代它；差异以本文为准。

---

## 0. 当前主线状态

| 项 | 值 |
|---|---|
| 当前 `main` HEAD | `6624efb`（含 `28ca859` S4 报告生成器、`1d69410` scheduler 续跑修复、`d32594d` 本文初版） |
| 已合并功能线 | T0（S1 PDF）、T8（S2 多投标人）、T2（S3 评审）、T3（并发调度）、**S4 数据层（T6 前半）**、T1/T7（业务配置）、T9（性能实测） |
| 测试 | `pytest` 全绿（**109 项**，含新增 `test_s4_report.py`；旧数 81 passed 是 S4 合入前的） |
| 分支关系 | `feat/backend-prototype`、`feat/t2-s3-review`、`prototype-t8-seed` 的功能均已进 `main`。**例外：`fix/prototype-criteria` 还有一个未合并提交（`35b8275` 前端跟上 criteria 接口变更）**；远端 `feat/t2-s3-review` 晚间又推了新提交（`166e8c3`），合并前需看一眼是什么 |

**结论：四条线 + S4 数据层在代码层面全部收口，主干测试全绿。剩 T6 前端半边的验收与两个已知缺口（§1.4）。**

---

## 1. 任务状态总表

### 1.1 后端 · 数据管道（已实现线）

| 任务 | 状态 | 完成判定依据 | 备注 |
|---|---|---|---|
| **T0** S1 支持 PDF 切块 | ✅ 已完成 | `5668d40` + README §7；240 PDF 跑通 32,129 块 / 39.4s；`page` 字段已补 | 无遗留 |
| **T8** S2 适配多投标人 + 单文件检索 | ✅ 已完成 | `8dee0c8` `4bc1a80`；检索收窄到单 PDF、DF 统计基取该家全部 20 个 PDF（红线一）；输出补齐 `bidder`/`item_id`/`item_guid`；§6 阶段一三条指标全达标 | 无遗留 |
| **S2 换行匹配修复** | ✅ 已完成 | `e3cad39`；跨 LF/CRLF 回归测试；原始 `text` 保留 | 今日补，独立于原计划编号 |
| **T3** 并发调度 + 断点续跑 | ✅ 已完成 | `5decaf7` → 合入 main；manifest 原子落盘、断点续跑验证过；`1d69410` 修复「续跑丢结果」+ Windows 落盘加固 | **注意**：工作区 `scheduler.py` 晚间又出现未提交改动，收口前需清点 |

### 1.2 模型侧 · S3 与性能实测

| 任务 | 状态 | 完成判定依据 | 备注 |
|---|---|---|---|
| **T2** S3 评审引擎 | ✅ 已完成 | `35af2a0` + `6f0cc0f`(#14)；重试/未评定/两种未命中/`cite` 越界全实现；要素加权定分已拆除 | 无遗留 |
| **T9** 模型单点性能实测 | ✅ 已完成 | **直接证据**：README §6 阶段二 12 家 228 项串行实测 8.24 分钟、0 未评定；`run_full_12.py` 可复现（端点接入见 `b4676cd`，解除 B2） | B2 解除的硬证据 |

### 1.3 Aiden · 清障、决策、业务配置

| 任务 | 状态 | 完成判定依据 / 待办 |
|---|---|---|
| **T1** 评分表 + 项目特征摘要 | ✅ 已完成 | `c6550ef` 补 19 项 `criteria`；`config/projects/济阳区实验高级中学.yaml` 产出 |
| **T7** 检索词与同义词 | 🟡 **待人工确认** | 229 条已填、实测未命中/降级归零；**「人工检查过输出」未做，按 §2.2 不能标完成**（初版标 ✅ 与 §4 待办第 4 条自相矛盾，已校正） |
| **B2** 模型端点 | ✅ 已解除 | 智能体工厂端点，T9 实测为硬证据；端点不返回 token 计数，改本地估算并在报告标注 |
| **B3** 算力规格 | 🟡 **待补数据** | 归属已定（神思自有算力，8/20 拍板）、远程托管口径 README §6 已备好；**缺的只是卡型号与显存数字**，不是待拍板 |
| **B5** 后端人选 | ⚠️ 文档未同步 | 后端任务实际均完成，`分工与排期.md` §3.3 措辞待回填 |
| **验收签字** | ⚠️ 待项目经理判定 | 按 §3.3 应由 Aiden 判定，非实现者自评 |

### 1.4 前端 · 界面与报告（2026-08-20 晚前端回报 + 本仓核实）

**注意：前端在另一台机器（`E:/chuanweibiao-agent`）作业。** 其回报里的「未提交修改」（`app.js` 的 `tierSummary()`、页面②三档区间摘要）**核实已在 main 中**（`a27a471`，15:33）——**前端在那台机器上继续改之前必须先 pull**，否则拿旧底稿改。

| 任务 | 状态 | 完成判定依据 / 待办 |
|---|---|---|
| **T5** 页面①②③ | 🟡 实现基本成型，待验收 | 五页 Hash 路由可点；实时滚动日志、计时、重试、未评定、token、「显存未采集」口径都有。等 Aiden 按验收栏判定 |
| **T6** 页面④⑤ + 报告导出 | 🟡 实现基本成型，待验收 + 一个硬缺口 | S4 数据层已提交（`28ca859`，report.json + report.md）；12×19 矩阵、横向滚动、固定列、`0`/`—` 区分、详情页证据引用、专家改判记录、浏览器本地导出静态 HTML（含 `compute_notes`）均已实现。**硬缺口：`miss_reason` 的 0⚠ 二级渲染未落地——`not_found` 必须显示为 0⚠ 人工复核，这是红线三配套、ui-spec §5 防回退项，验收不过就得补**。页面⑤ PDF 页跳转高亮未做——在砍单列表第 1 位，可砍 |
| **造 mock** | ✅ 已完成 | **销毁条件已触发（B2 解除、T2 跑通）**，确认 `data/interim/mock/` 是否已删 |

---

## 2. 阻塞项总览（README §8）

|B#|事项|状态|说明|
|---|---|---|---|
|B1|真实文件未拿到|✅ 已解除（8/19）|`原始资料/实际测试工程文件/` 已在手|
|B2|模型端点|✅ 已解除|T9 实测即硬证据|
|B3|算力规格|🟡 待补数据|归属已定、口径已备，缺卡型号/显存数字|
|B4|命名不符实际内容|⚠️ 待澄清|非技术阻塞，对外口径问题|
|B5|后端人选|⚠️ 实际已做，文档未同步|回填人名即可|
|B6|前端技术栈|⚠️ 静态原型阶段不阻塞|确定后写入 `ui-spec.md` §2|
|B7|评审点原始资料缺陷|✅ 降级非阻塞|评审点库本项目不用|
|B8|S1 不支持 PDF|✅ 已解除|T0 完成|

---

## 3. 与 `分工与排期.md` 的差异

`分工与排期.md` 文首已加状态横幅指向本文（2026-08-20 晚）；§3/§4 的 8/19 措辞保留为历史记录，**当前状态以本文为准**。

---

## 4. 项目经理待办（按优先级）

| # | 动作 | 负责人 | 风险 |
|---|---|---|---|
| 1 | ~~回填 `分工与排期.md` 状态~~ **已做（横幅式）** | Aiden | — |
| 2 | B3 补卡型号/显存数字（采不到按远程托管口径写「未采集」） | Aiden + 客户 | 影响报告 `compute_notes` |
| 3 | 前端 T5/T6 验收判定；**T6 先补 `miss_reason` 0⚠ 二级渲染再验** | 前端 + Aiden | 原型提交 ≠ 验收通过 |
| 4 | T7 synonyms 与后端/模型侧产出逐项人工核对签字 | Aiden | README §2.2 硬性要求 |
| 5 | **8/21 上午全量演练**（12 家 × 19 项计时，界面 + 计时 + 客户视角） | 全员 | 验收动作本身，不可砍 |
| 6 | 仓库收尾：处理 `fix/prototype-criteria` 分支；清点工作区未提交改动（`data-contract.md`、`scheduler.py`、`test_s3_scheduler.py`、`.gitignore`）；确认远端 `feat/t2-s3-review` 新提交（`166e8c3`）是什么；确认 mock 删除 | 后端 + Aiden | 多 session + 两台机器并行，不收尾会互相覆盖 |

---

## 5. 一句话结论

**代码层面含 S4 数据层全部收口、主干 109 项测试全绿。剩余的是收口动作：B3 补数、前端验收（先补 0⚠）、T7 与产出签字、mock 清理与分支/工作区收尾——以及 8/21 上午那场决定交付成败的全量演练。**
