# CLAUDE.md — 工程建设类技术标辅助评审系统

> 给接手本项目的 AI 协作者。本文件只放**项目专属契约**，事实一律不重复。
> 项目是什么、术语、架构、任务、阻塞项，全部在 README.md，那是唯一事实来源。

@README.md

---

## 动手前必读

1. **README §2.1**：评分项与投标文件是 GUID 绑定的。这是本项目最重要的结构事实，
   不懂它就理解不了为什么检索问题被大幅简化、为什么 114 个评审点用不上。
2. **README §3.1**：当前实现与目标状态有六处差异，其中 **S1 不支持 PDF 而真实文件全是 PDF**。
   不看这节，跑完命令会对着输出发懵。
3. **README §3.2 三条红线**：踩坑得出的结论，每条配了回归测试和复现方式。
   红线二第二半（锚点 DF=0 时不换词，直接判未命中）反直觉，是一次错误修复换来的——
   想「优化」它之前，先按那节的复现方式跑一遍看看会坏成什么样。
4. **README §0 已定的边界**：只做每家独立打分，不做跨家对比。这条已经拍过板，别再往回提。

## 硬规矩

**改 `src/s2_locate.py` 必须跑测试，绿了才算完。**

```bash
python -m pytest tests/ -q
```

测试挂了先看是不是踩了红线，不要直接改断言迁就代码。断言写的是设计意图，代码写的是当前实现，
两者冲突时**默认代码错**。确实是设计要变，先改 README §3.2，再改断言，最后改代码。

**改 README §4 的数据契约 = 改模块间接口。** 上下游要同步改，改完在 §4 里把字段标注更新掉
（哪些是「设计」哪些已实现）。字段名在文档和代码里必须一致——曾经出现过 `score` 一名两义
（检索分 / 判分），现在检索分叫 `match_score`，别改回去。

**未解决项写进 `docs/issues.md`，不要塞进 README。** 分级判据与格式见 `docs/goal-readme-校准.md`。
README 只写已定的事实与设计，待决策的事项留在 issues 里，两处不重复。

**`原始资料/` 只读。** 甲方和公司给的原件，不改、不移、不覆盖。需要衍生文件就往 `data/` 或项目根写。

**魔数要归零。** 阈值必须是相对量（DF 占比这种），不能是绝对值——绝对值会随语料规模漂移，
换一批标书就得重调。这条同样有测试守着（`test_阈值不随语料规模漂移`）。

## 报进度的规矩

README §2.2 定义了「已完成 / 进行中 / 未开始」的判定标准，**已完成 = 代码写了 + 代表性数据跑通 +
人工检查过输出，三者缺一不算**。别把「代码写完了」说成「做完了」。

同理，README 里凡是写了「完成判定」的任务，没达到那个判定就不要标完成。

## 写文档的规矩

这份项目的文档要过冷读：**一个没有任何上下文的人或 AI，只读那一个文件，
能答出「这在讲什么」「每个术语什么意思」「我接下来做什么」。**

具体到本项目已经踩过的坑：

- 术语、编号规则（`①-1` 这种）、字段前缀（`t:` `b:` 这种）必须在同一文件内定义
- 指标要有单位和方向（「压缩比 4 倍，越大越好」，不是「压缩比不错」）
- 待办要有验收条件，阻塞项要有解除条件（「拿到 base_url」不算，「能跑通一次调用」才算）
- 引用要指向确定的去处，别写「之前那版」「上次讨论的方案」
- 示例数据要标注是占位值，否则会和真实约束打架（曾经界面里写死 `A100 40G`，
  而算力规格实际还没定）

改完文档自己冷读一遍。份量大的时候，开一个干净 agent 只喂这一个文件，让它回答上面三个问题，
看它栽在哪——比自评可信得多，本项目已经用这个方法抓出过五处硬伤。
